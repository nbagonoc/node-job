{{> guest/_head}}
<div class="container my-3">
    <div class="row">
        <div class="col-lg-3 col-md-4">
            {{> user/_sidebar}}
        </div>
        <div class="col-lg-9 col-md-8 pl-md-1">
            {{> user/_messages}}{{{body}}}
        </div>
    </div>
</div>
{{> guest/_footer}}